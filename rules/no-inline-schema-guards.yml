id: no-inline-schema-guards
language: TypeScript
severity: error
message: Bind Schema-generated guards where the model is defined (for example, const isCompaction = SessionEntry.guards.Compaction) instead of calling generated guards inline.
files:
  - src/**/*.ts
  - test/**/*.ts
rule:
  pattern: $SCHEMA.guards.$GUARD($$$ARGS)
